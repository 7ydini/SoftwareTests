package steps;

public class MyStepdefsD implements En {
    public MyStepdefsD() {
        Допустим("^введены два числа <a> и <b>$", () -> {
        });
        Если("^разделить$", () -> {
        });
        То("^получим <результат>$", () -> {
        });
        Допустим("^введено только первое число <a>$", () -> {
        });
        То("^произойдет <ошибка>$", () -> {
        });
        Допустим("^введено только второе число <b>$", () -> {
        });
    }
}
